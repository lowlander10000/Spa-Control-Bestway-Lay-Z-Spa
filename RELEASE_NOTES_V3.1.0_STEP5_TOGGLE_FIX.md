# Spa Control v3.1.0 – Step 5 Toggle Fix

- Toggle buttons without a state topic now preserve their local ON/OFF state.
- The two-second state poll no longer resets toggles that have no state topic.
- Toggle labels immediately show Aan/Uit after a successful MQTT publish.
- PWA asset version increased to force loading the corrected JavaScript.
